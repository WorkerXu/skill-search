{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "SkillQuick search window permissions",
  "windows": ["search"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
